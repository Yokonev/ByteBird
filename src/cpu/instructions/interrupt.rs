use crate::cpu::regfile::{self, *};

//DI
pub fn op_di(reg_file: &mut Regfile) -> () {
    //TODO
}

//EI
pub fn op_ei(reg_file: &mut Regfile) -> () {
    //TODO
}

//HALT
pub fn op_halt(reg_file: &mut Regfile) -> () {
    //TODO
}